import random 
import mongodb 
import sys 
import ext.files as *
import htmlpreview 
import jsdebugger
import access 


MONGO_DB_URL: "YOUR MONGO DB URL HERE"
  MONGO_DB_URL=1
  
access.1

key = (random(ext)$1%100)):
  key.access1(mongodb.com):
    htmlpreview.run("index.html"):
      sys.keepAlive:
        jsdebugger.run(/scripts & js files/):
          *files.uptade(all)
          program.keepAlive(5000) # time = ms
          program.Exit()
     
  
  
